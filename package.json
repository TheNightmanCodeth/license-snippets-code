{
	"name": "license-helper-code",
	"displayName": "License Helper",
	"description": "Configurable license and file-header snippets with support for many popular open source licenses.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.40.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onCommand:license_helper.firstLaunch"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": {
			"title": "License Helper",
			"properties": {
				"licenseHelper.author": {
					"type": "string",
					"default": "Author",
					"markdownDescription": "The value for the `author` field in file headers."
				},
				"licenseHelper.organization": {
					"type": "string",
					"default": "Organization",
					"markdownDescription": "The value for the `organization` field in file headers."
				},
				"licenseHelper.projectName": {
					"type": "string",
					"default": "Project Name",
					"markdownDescription": "The value for the `project name` field in file headers."
				},
				"licenseHelper.contactEmail": {
					"type": "string",
					"default": "<contact@email.com>",
					"description": "The value for the project contact email in file headers."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.7",
		"@types/node": "^12.11.7",
		"@types/vscode": "^1.40.0",
		"glob": "^7.1.5",
		"mocha": "^6.2.2",
		"typescript": "^3.6.4",
		"tslint": "^5.20.0",
		"vscode-test": "^1.2.2"
	},
	"dependencies": {
		"ts.data.json": "^0.3.0"
	}
}
